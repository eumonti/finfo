#include <stdio.h>
#include <time.h>

// typedef struct
// {
//   int x;
//   int y;
// } Posizione;

int main()
{
  // Posizione p = {.x = 10,
  //                .y = 20};

  // printf("x: %d\ny: %d\n", p.x, p.y);
  time_t t = time();
  printf("%ul")
}